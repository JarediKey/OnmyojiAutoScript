# DailyTrifles

[English](README.md)

商店操作结束后，任务识别当前页面并导航回庭院。由于返回操作可能直接到庭院，不再强制等待中间的商店页面。购买及签到设置不变。

恢复逻辑检查：`python -m unittest discover -s tests -p test_login_and_secret_recovery.py -v`。离线检查不能代替游戏实测。
